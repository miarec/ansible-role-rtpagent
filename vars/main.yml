---

rtpengine_source_dependencies:
  - git
  - dkms
  - build-essential
  - cmake
  - curl
  - gperf


  - libglib2.0-dev
  - zlib1g-dev
  - libssl-dev
  - libevent-dev
  - libpcre3-dev
  - libxmlrpc-core-c3-dev
  - libjson-glib-dev
  - libwebsockets-dev
  - libnftnl-dev
  - libmnl-dev
  - libsystemd-dev
  - libhiredis-dev
  - libspandsp-dev
  - libpcap-dev

  # curl is required by test
  - libcurl4-openssl-dev

  # Pandoc is required for "make install" target (documentation is generated)
  - pandoc


# "iptables" is deprecated module. Use nftable instead
# rtpengine_source_iptables_dependencies:
#  - libiptc-dev

# Transcoding is disabled in this project
# rtpengine_source_transcoding_dependencies:
#   - libswresample-dev
#   - libavcodec-dev
#   - libavfilter-dev
#   - libavformat-dev
#   - libavutil-dev
#   - libopus-dev
#   - libopus0
#   - opus-tools
#   - default-libmysqlclient-dev
